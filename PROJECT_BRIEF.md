# 对得上 DuiDeShang - 项目规格

## 1. 项目目标

这是一个用于 FDE 实习面试演示的 Web 产品。

一句话定位：

> 面向广告制作门店的需求澄清、方案确认与可解释报价助手。它将客户的模糊口语需求转化为双方确认的规格、报价构成、交付边界和验收标准，减少默认理解、返工和客户不满意。

核心价值不是帮助熟练店家更快算出价格，而是解决客户与商家之间的需求对接问题：

- 发现客户表达中不清晰、有多种合理解释的部分。
- 不允许商家经验或 AI 推断被悄悄当成客户已经确认的需求。
- 把专业概念转化为客户能够理解和选择的文字或视觉方案。
- 在报价前锁定需求，在交付前形成可验证的验收标准。
- 将商家脑中的经验报价转化为客户看得懂的价格组成和交付边界。

典型问题：客户说“字体正规一点”，老板可能默认使用黑体，但客户实际期待宋体。即使价格准确、施工合格，最终仍会因为理解偏差导致返工和不满意。

## 2. 目标用户和场景

MVP 只服务广告制作门店的门头询价场景，不扩展到其他行业。

典型客户输入：

> 想做个三米左右的门头，正规大气一点，要能发光，预算四五千，下周五前弄好。

这段话包含：

- 明确信息：门头、约 3 米、需要发光、预算 4000-5000 元。
- 模糊信息：“三米左右”“正规大气一点”。
- 交付歧义：“下周五前”可能指完成设计、完成制作或安装验收。
- 缺失信息：安装高度、墙面材质、字体方案、发光方式、旧招牌拆除、电源条件、是否含税等。

## 3. 核心业务流程

1. 用户粘贴客户微信询价。
2. 系统提取明确需求。
3. 系统识别模糊项、缺失项和未经确认的假设。
4. 系统按返工风险和价格影响排列澄清问题。
5. 系统用客户能理解的文字和视觉选项协助客户做决定。
6. 客户逐项确认，形成实时需求规格。
7. 所有高风险字段确认后，由确定性报价引擎计算价格。
8. 系统生成需求确认单、价格组成、交付边界、验收标准和微信确认话术。
9. 后续修改已确认需求时，保留需求变更记录并重新计算报价。

## 4. Agent 与程序的职责边界

Agent 负责：

- 理解客户口语。
- 引用原文提取需求。
- 发现歧义和缺失信息。
- 生成澄清问题。
- 将专业术语转换成客户能理解的选项。
- 生成需求复述、确认话术和验收说明草稿。

确定性程序负责：

- 字段状态管理。
- 判断高风险字段是否全部确认。
- 读取行业知识库中的价格和规则。
- 计算数量、单价、附加费、税费和总价。
- 记录需求版本和报价版本。
- 导出确认单。

硬性原则：

- AI 不得心算正式报价。
- AI 不得凭空创造材料单价。
- AI 推断不能自动变成客户确认。
- 高风险字段未确认时不得生成“正式报价”。
- 报价必须保留人工复核入口。

## 5. MVP 功能范围

### P0 - 必须实现

- 粘贴客户询价文本。
- 提取明确、模糊、缺失、建议和已确认需求。
- 在原始文本中高亮模糊表达。
- 生成澄清问题和选项卡。
- 提供字体、发光方式等简单视觉选择。
- 用户逐项确认，右侧需求确认单实时更新。
- 高风险字段未确认时锁定正式报价按钮。
- FastAPI 确定性报价引擎。
- 报价明细、计价依据、假设和不包含项目。
- 可验证的验收标准。
- 需求变更历史。
- 可复制的微信确认话术。
- 浏览器打印或导出。
- 无外部 AI 接口也能运行的演示模式。

### P1 - 有余力再实现

- 上传现场照片。
- 更多字体和发光效果预览。
- “客户预算只有 4000 元”替代方案。
- 报价版本对比。
- 一键复制客户回复。

### 明确不做

- 微信自动接入。
- 自动抓取供应商或平台数据。
- 在线支付、电子签约。
- 多商户、多行业。
- 用户账户和复杂权限。
- 向量数据库。
- 未经人工确认自动向客户发送报价。

## 6. 技术栈与目录

- 前端：Next.js App Router、TypeScript、Tailwind CSS、shadcn/ui。
- 后端：FastAPI、Pydantic。
- 行业知识：YAML 或 JSON 规则库。
- MVP 暂不使用数据库；会话状态可保存在前端状态和 localStorage。
- 前后端通过 REST API 通信。
- AI 服务封装成可替换 Provider，默认启用本地演示 Provider。

计划目录：

```text
apps/
  web/
  api/
apps/api/knowledge/
docs/
PROJECT_BRIEF.md
CODEX_START.md
README.md
```

环境变量：

- `NEXT_PUBLIC_API_BASE_URL`
- `CORS_ORIGINS`
- 可选的模型 Provider 和 API Key；密钥不得写入源码或提交 Git。

部署方向：

- GitHub 保存 monorepo。
- Next.js 前端部署到 Vercel。
- FastAPI 作为独立 Python 后端部署。
- 前端不可达后端或 AI 失败时，仍可切换演示模式。

## 7. 界面设计

界面应像真实、克制的中文 SaaS 产品，不做成普通聊天机器人。

桌面端采用三栏布局：

- 左侧：客户原始对话。明确、模糊和缺失信息用不同颜色表示。
- 中间：澄清问题、文字或视觉选项、客户确认操作。
- 右侧：实时需求规格、确认进度、风险、报价状态和验收摘要。

移动端降级为分步骤布局。

推荐组件：

- `CustomerMessageInput`
- `RequirementHighlight`
- `ClarificationCard`
- `VisualOptionCard`
- `RequirementSheet`
- `QuoteBreakdown`
- `AcceptanceChecklist`
- `ChangeHistory`
- `DemoCaseSelector`

客户不一定理解行业术语。例如“正规一点”不应只问“黑体还是宋体”，而应展示：

- 稳重正式：类似机构门牌的宋体风格。
- 简洁现代：笔画均匀、远距离清晰的黑体风格。
- 传统文化：具有书写感的书法风格。

## 8. 数据模型

需求字段至少包含：

```text
field_name
display_name
value
status
source_text
confidence
risk_level
affects_price
affects_delivery
clarification_question
options
confirmed_by
confirmed_at
```

状态至少包含：

```text
explicit
ambiguous
missing
suggested
confirmed
changed
```

报价项至少包含：

```text
item_name
specification
quantity
unit
unit_price
subtotal
pricing_rule
price_source
updated_at
assumptions
```

AI 分析使用结构化输出并通过 Pydantic 校验，至少返回：

```json
{
  "explicit_requirements": [],
  "ambiguities": [],
  "missing_requirements": [],
  "suggested_questions": [],
  "unsupported_assumptions": []
}
```

## 9. 行业知识库

MVP 不使用 RAG 或向量数据库，使用可读、可审计的 YAML/JSON 规则。

```text
apps/api/knowledge/
  products.yaml
  materials.yaml
  clarification_rules.yaml
  pricing_rules.yaml
  acceptance_templates.yaml
  ambiguity_dictionary.yaml
  demo_cases.yaml
```

第一版只支持：

- 亚克力正面发光字。
- 不锈钢背发光字。
- PVC 字。
- 灯箱门头。

知识库需要描述：

- 可选材料和常用规格。
- 发光方式。
- 计价单位。
- 影响价格和交付的参数。
- 必须询问的问题。
- 常见施工风险。
- 验收方式。

所有价格均为“演示门店内部价目”，不得宣称为市场统一价格。界面固定显示：

> 当前价格仅用于产品演示，真实项目需由商家维护价目并人工确认。

报价规则至少支持：

- 按平方米。
- 按数量。
- 固定服务费。
- 最低起步价。
- 拆除费。
- 运输费。
- 高空安装附加费。
- 加急系数。
- 税费。
- 人工调整并记录原因。

## 10. 演示案例和验收

至少覆盖：

1. “正规大气一点，三米左右。”
2. “跟隔壁那个差不多，但是高级一点。”
3. “预算四千，材料你看着办。”
4. “下周五之前做好。”
5. “我要黑体。”
6. “就在二楼装，应该不高。”

必须验证：

- 不把模糊需求自动标记为已确认。
- 每个澄清问题能够引用客户原话。
- 客户明确说“黑体”时不重复询问字体类型。
- 高风险字段未确认时不能生成正式报价。
- 相同确认输入始终产生相同报价。
- 修改已确认字段后生成变更记录。
- 不使用知识库之外的价格。
- 演示模式无需 API 密钥即可完整运行。
- 前后端 lint、类型检查、测试和生产构建通过。
- 浏览器中桌面端和移动端主流程没有溢出、遮挡和控制台错误。

## 11. 最终交付

- 可本地运行的完整项目。
- `README.md`：产品定位、架构、启动、环境变量和演示步骤。
- `.env.example`。
- 示例数据和演示模式。
- 自动化测试。
- `docs/architecture.md`。
- `docs/demo-script.md`：3-4 分钟面试演示脚本。
- `docs/limitations.md`：产品边界和风险。
- `docs/deployment.md`：GitHub、Vercel 前端和 FastAPI 后端部署准备。
- 后续中文项目报告。

报告必须诚实区分：已实现功能、模拟数据、待验证商业假设和未来能力。不得虚构用户访谈、转化率、节省时间或商业效果。

## 12. 面试叙事

项目迭代过程是重要成果：

> 快速报价工具 -> 可解释报价 -> 发现真正痛点是需求对齐 -> 构建澄清、确认、报价和验收闭环。

FDE 认知：不是拿固定产品寻找客户，而是进入真实工作流，找到信息丢失、理解偏差和责任模糊的环节，再用最小原型验证是否值得改造。
