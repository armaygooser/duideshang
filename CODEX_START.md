# Codex 开发启动指令

请先完整阅读当前工作区根目录的 `PROJECT_BRIEF.md`，并把它作为产品目标、范围和验收标准的主要事实来源。

任务：在当前工作区实现 `PROJECT_BRIEF.md` 描述的“对得上 DuiDeShang”面试演示产品。

执行方式：

1. 检查当前工作区和已有文件，保留所有与任务无关的用户文件。
2. 先给出简短实施计划，然后持续完成开发、运行和验证，不要只返回代码片段或停留在设计阶段。
3. 建立 Next.js + TypeScript + shadcn/ui 前端、FastAPI + Pydantic 后端和 YAML/JSON 行业规则库。
4. 优先完成“客户原话 -> 识别歧义 -> 逐项确认 -> 确定性报价 -> 验收标准”的 P0 完整闭环。
5. 提供不依赖外部 API 的演示模式，真实模型接入只能作为可选 Provider。
6. 用程序计算价格，禁止让大模型生成或心算正式报价。
7. 实际运行 lint、类型检查、测试、生产构建和浏览器流程验证；修复发现的问题。
8. 完成 README、环境变量示例、架构说明、演示脚本、局限说明和部署准备文档。

边界：

- 不扩展微信接入、支付、电子签约、多商户、多行业、复杂权限或向量数据库。
- 不伪造真实行业数据。
- 不把密钥写入源码。
- 不删除或覆盖 `参考资料` 目录。
- 未经用户明确确认，不推送 GitHub、不创建远程仓库、不部署云端服务。
- 遇到不影响核心目标的小选择时采用合理默认值继续推进。

完成条件：

- `PROJECT_BRIEF.md` 中 P0 功能和测试标准均已满足。
- 前后端能够从全新环境按 README 启动。
- 面试现场即使没有模型 API 或网络不稳定，也能完成预设演示。
- 最终汇报实际完成内容、启动命令、测试结果、剩余限制和发布前所需事项。
